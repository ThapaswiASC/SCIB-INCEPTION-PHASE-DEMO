# UI COMPONENT SPECIFICATIONS

## 1. INPUT REFERENCE (FROM AGENT-1)

**Page Name:** Kanban Board Dashboard

**Mapped Layout:** Three-column CSS Grid layout with sidebar navigation and header

**Components from A1:**
- KanbanBoardComponent (main container)
- KanbanColumnComponent (individual columns)
- TaskCardComponent (task display)
- TaskModalComponent (task creation/editing)
- HeaderComponent (search and user actions)
- SidebarComponent (navigation and branding)

## 2. GLOBAL ARCHITECTURE SUMMARY

**Layout Type:** Mixed Grid/Flex
- **Primary:** CSS Grid for kanban columns (grid-template-columns: repeat(3, 1fr))
- **Secondary:** Flexbox for header, navigation, and card layouts
- **Responsive:** Breakpoint adaptations for desktop (>1024px), tablet (768px-1024px), mobile (<768px)

**Main Sections:**
- Header (search, notifications, user profile)
- Sidebar (brand, navigation, quick actions)
- Main Content (page header, kanban board)
- Modals (task creation/editing)

**Component Strategy:**

**Reused Components:**
- aava-button (from uicomplibraryplay)
- aava-dialog (from uicomplibraryplay)
- aava-popup (from uicomplibraryplay)
- aava-popover (from uicomplibraryplay)

**New Components (if any):**
- KanbanBoardComponent
- KanbanColumnComponent
- TaskCardComponent
- TaskModalComponent
- HeaderComponent
- SidebarComponent

## 3. FOLDER STRUCTURE

src/
 ├── app/
 │    ├── features/
 │    │    └── kanban/
 │    │         ├── components/
 │    │         │    ├── kanban-board/
 │    │         │    │    ├── kanban-board.component.ts
 │    │         │    │    ├── kanban-board.component.html
 │    │         │    │    ├── kanban-board.component.scss
 │    │         │    │    └── kanban-board.component.spec.ts
 │    │         │    ├── kanban-column/
 │    │         │    └── task-card/
 │    │         ├── services/
 │    │         ├── models/
 │    │         └── kanban.module.ts
 │    ├── shared/
 │    │    ├── components/
 │    │    │    ├── header/
 │    │    │    ├── sidebar/
 │    │    │    ├── task-modal/
 │    │    │    └── ui-library/
 │    │    ├── services/
 │    │    └── models/
 │    └── core/

## 4. COMPONENT IMPLEMENTATION

### 4.1 KanbanBoardComponent

**Component Type:** Feature

**Mapped From A1:** Main container for three-column kanban layout

**Purpose:** Primary container managing kanban board state and three-column layout with drag/drop functionality

**Parent:** ContentAreaComponent

**Children:**
- KanbanColumnComponent (To Do)
- KanbanColumnComponent (In Progress)
- KanbanColumnComponent (Done)

**Dependencies:**
- KanbanService
- TaskService
- Angular CDK Drag Drop

**Library Components Used:**
- aava-button → Action buttons
- aava-dialog → Error dialogs

**TypeScript Specification**

**Inputs:**
- loading: boolean (loading state indicator)
- error: string | null (error message display)

**Outputs:**
- taskSelected: EventEmitter<Task> (task selection events)
- taskMoved: EventEmitter<TaskMoveEvent> (task drag/drop events)
- columnAction: EventEmitter<ColumnActionEvent> (column-level actions)

**State:**
- columns: KanbanColumn[] (column data with tasks)
- isLoading: boolean (loading state)
- errorMessage: string | null (error handling)

**Methods:**
- ngOnInit() → Initialize component and load data
- loadKanbanData() → Fetch initial kanban data
- onTaskMoved(event: CdkDragDrop<Task[]>) → Handle task drag/drop
- onTaskSelected(task: Task) → Handle task selection
- handleError(error: any) → Error handling

**HTML STRUCTURE (PSEUDO CODE ONLY)**

```html
<div class="kanban-board-container" role="region" aria-label="Kanban Board">
  <div class="kanban-header">
    <h2>Task Board</h2>
    <aava-button variant="primary" (click)="openCreateTaskModal()">Create Task</aava-button>
  </div>
  
  <div class="kanban-grid" cdkDropListGroup>
    <div class="kanban-column" *ngFor="let column of columns">
      <app-kanban-column 
        [column]="column"
        [allowDrop]="true"
        (taskDropped)="onTaskMoved($event)"
        (taskSelected)="onTaskSelected($event)">
      </app-kanban-column>
    </div>
  </div>
  
  <div class="error-container" *ngIf="errorMessage">
    <aava-dialog [isOpen]="!!errorMessage" (closed)="clearError()">
      <p>{{ errorMessage }}</p>
    </aava-dialog>
  </div>
</div>
```

**CSS SPECIFICATION**

**Layout:**
- .kanban-board-container: flex column layout
- .kanban-grid: display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem

**Spacing:**
- padding: 1.5rem
- gap: 1rem between columns

**Component Styles:**
- .kanban-header: flex justify-between align-center
- .kanban-column: min-height: 500px
- .error-container: position fixed for modal overlay

**Responsive:**
- Desktop (>1024px): 3 columns side by side
- Tablet (768px-1024px): 2 columns with scroll
- Mobile (<768px): 1 column stacked

**API INTEGRATION**

- loadKanbanData() → GET /api/youth-accounts/{id}/dashboard → Load initial kanban data
- onTaskMoved() → PUT /api/tasks/{id}/status → Update task status on move

**ERROR HANDLING**

- API failure → show error dialog with retry option
- Validation error → inline column error messages
- Network timeout → show connection error with offline mode

**INTERACTION FLOW**

**User Action → UI → API**
- Component init → load columns → GET /api/dashboard → display kanban board
- Drag task → update UI → PUT /api/tasks/{id} → sync status
- Click create → open modal → POST /api/tasks → refresh board

### 4.2 KanbanColumnComponent

**Component Type:** Feature

**Mapped From A1:** Individual column with header and task list

**Purpose:** Manages individual kanban column with drag/drop zone and task list display

**Parent:** KanbanBoardComponent

**Children:**
- TaskCardComponent[] (multiple task cards)
- ColumnHeaderComponent

**Dependencies:**
- Angular CDK Drag Drop
- TaskService

**Library Components Used:**
- aava-button → Column action buttons
- aava-popover → Column options menu

**TypeScript Specification**

**Inputs:**
- column: KanbanColumn (column configuration and data)
- allowDrop: boolean (enable drag/drop functionality)
- readonly: boolean (read-only mode)

**Outputs:**
- taskDropped: EventEmitter<TaskDropEvent> (task drop events)
- addTask: EventEmitter<string> (add task to column)
- columnOptionsClicked: EventEmitter<string> (column menu actions)
- taskSelected: EventEmitter<Task> (task selection from child)

**State:**
- tasks: Task[] (tasks in this column)
- isDropTarget: boolean (drag/drop state)
- showOptions: boolean (column options menu)

**Methods:**
- onTaskDrop(event: CdkDragDrop<Task[]>) → Handle task drop
- onAddTask() → Trigger add task event
- toggleOptions() → Show/hide column options
- onTaskClick(task: Task) → Forward task selection

**HTML STRUCTURE (PSEUDO CODE ONLY)**

```html
<div class="kanban-column" [attr.aria-labelledby]="column.id + '-header'">
  <div class="column-header" [id]="column.id + '-header'">
    <h3>{{ column.title }}</h3>
    <span class="task-count">{{ column.tasks.length }}</span>
    <aava-button variant="ghost" (click)="toggleOptions()" aria-label="Column options">
      <i class="icon-more"></i>
    </aava-button>
    <aava-popover [isOpen]="showOptions" (closed)="showOptions = false">
      <button (click)="onAddTask()">Add Task</button>
      <button (click)="clearColumn()">Clear Column</button>
    </aava-popover>
  </div>
  
  <div class="task-list" 
       cdkDropList 
       [cdkDropListData]="column.tasks"
       (cdkDropListDropped)="onTaskDrop($event)"
       [class.drop-target]="isDropTarget">
    <app-task-card 
      *ngFor="let task of column.tasks; trackBy: trackByTaskId"
      [task]="task"
      [draggable]="allowDrop && !readonly"
      (taskClicked)="onTaskClick(task)"
      cdkDrag>
    </app-task-card>
    
    <div class="empty-state" *ngIf="column.tasks.length === 0">
      <p>No tasks in {{ column.title }}</p>
      <aava-button variant="secondary" (click)="onAddTask()" *ngIf="!readonly">
        Add First Task
      </aava-button>
    </div>
  </div>
</div>
```

**CSS SPECIFICATION**

**Layout:**
- .kanban-column: flex column, min-height: 500px
- .task-list: flex column with gap, flex-grow: 1

**Spacing:**
- padding: 1rem
- gap: 0.75rem between tasks

**Component Styles:**
- .column-header: flex justify-between, border-bottom
- .drop-target: border-dashed, background highlight
- .empty-state: text-center, padding: 2rem

**Responsive:**
- Desktop: full height columns
- Tablet: scrollable columns
- Mobile: stacked layout

**API INTEGRATION**

- No direct API calls (managed by parent KanbanBoardComponent)
- Data flows through @Input properties

**ERROR HANDLING**

- Drop validation → show inline error message
- Task load failure → show column-level error state
- Permission errors → disable drag/drop functionality

**INTERACTION FLOW**

**User Action → UI → API**
- Drop task → validate drop → emit taskDropped → parent handles API
- Click add task → emit addTask → parent opens modal
- Click options → show popover → emit columnOptionsClicked

### 4.3 TaskCardComponent

**Component Type:** Shared

**Mapped From A1:** Individual task display with all task information

**Purpose:** Displays individual task with priority, title, description, tags, assignee, and due date

**Parent:** KanbanColumnComponent

**Children:**
- None (leaf component)

**Dependencies:**
- DatePipe
- Angular CDK Drag Drop

**Library Components Used:**
- aava-popup → Task details tooltip
- aava-button → Quick action buttons

**TypeScript Specification**

**Inputs:**
- task: Task (task data object)
- draggable: boolean (enable drag functionality)
- selectable: boolean (enable selection)
- compact: boolean (compact display mode)

**Outputs:**
- taskClicked: EventEmitter<Task> (task selection)
- taskDragStart: EventEmitter<Task> (drag start)
- taskDragEnd: EventEmitter<Task> (drag end)
- quickAction: EventEmitter<{action: string, task: Task}> (quick actions)

**State:**
- isDragging: boolean (drag state)
- isSelected: boolean (selection state)
- showTooltip: boolean (tooltip visibility)

**Methods:**
- onClick() → Emit task selection
- onDragStart() → Set drag state and emit event
- onDragEnd() → Clear drag state and emit event
- onQuickAction(action: string) → Emit quick action
- getPriorityClass() → Return CSS class for priority
- formatDueDate() → Format due date display

**HTML STRUCTURE (PSEUDO CODE ONLY)**

```html
<div class="task-card" 
     [class.dragging]="isDragging"
     [class.selected]="isSelected"
     [class.compact]="compact"
     (click)="onClick()"
     (cdkDragStarted)="onDragStart()"
     (cdkDragEnded)="onDragEnd()"
     [attr.aria-label]="'Task: ' + task.title">
  
  <div class="task-header">
    <span class="priority-indicator" [class]="getPriorityClass()"></span>
    <h4 class="task-title">{{ task.title }}</h4>
    <aava-button variant="ghost" size="small" (click)="onQuickAction('edit')" *ngIf="!compact">
      <i class="icon-edit"></i>
    </aava-button>
  </div>
  
  <p class="task-description" *ngIf="task.description && !compact">
    {{ task.description | slice:0:100 }}{{ task.description.length > 100 ? '...' : '' }}
  </p>
  
  <div class="task-tags" *ngIf="task.tags?.length && !compact">
    <span class="tag" *ngFor="let tag of task.tags | slice:0:3">{{ tag }}</span>
    <span class="tag-more" *ngIf="task.tags.length > 3">+{{ task.tags.length - 3 }}</span>
  </div>
  
  <div class="task-footer">
    <div class="assignee" *ngIf="task.assignee">
      <img [src]="task.assignee.avatar" [alt]="task.assignee.name" class="avatar">
      <span class="assignee-name" *ngIf="!compact">{{ task.assignee.name }}</span>
    </div>
    
    <div class="due-date" *ngIf="task.dueDate" [class.overdue]="isOverdue(task.dueDate)">
      <i class="icon-calendar"></i>
      <span>{{ formatDueDate() }}</span>
    </div>
  </div>
  
  <aava-popup [isOpen]="showTooltip" [target]="taskCard" position="top">
    <div class="task-tooltip">
      <h5>{{ task.title }}</h5>
      <p>{{ task.description }}</p>
      <div class="tooltip-meta">
        <span>Priority: {{ task.priority }}</span>
        <span>Status: {{ task.status }}</span>
        <span *ngIf="task.dueDate">Due: {{ task.dueDate | date:'short' }}</span>
      </div>
    </div>
  </aava-popup>
</div>
```

**CSS SPECIFICATION**

**Layout:**
- .task-card: flex column, border-radius: 8px, box-shadow
- .task-header: flex align-center justify-between
- .task-footer: flex justify-between align-center

**Spacing:**
- padding: 1rem
- gap: 0.5rem between elements

**Component Styles:**
- .priority-indicator: width: 4px, height: 100%, border-left
- .task-tags .tag: inline-block, padding: 0.25rem 0.5rem, border-radius: 12px
- .avatar: width: 24px, height: 24px, border-radius: 50%
- .dragging: opacity: 0.5, transform: rotate(5deg)

**Responsive:**
- Desktop: full card with all details
- Tablet: compact mode with essential info
- Mobile: minimal card with title and assignee only

**API INTEGRATION**

- No direct API calls (data passed from parent)
- All interactions bubble up to parent components

**ERROR HANDLING**

- Missing task data → show placeholder content
- Image load failure → show default avatar
- Invalid dates → show "No due date" text

**INTERACTION FLOW**

**User Action → UI → API**
- Click card → emit taskClicked → parent opens task modal
- Start drag → emit taskDragStart → parent tracks drag state
- Quick edit → emit quickAction → parent opens edit modal

### 4.4 TaskModalComponent

**Component Type:** Shared

**Mapped From A1:** Task creation and editing form

**Purpose:** Modal dialog for creating new tasks and editing existing tasks with form validation

**Parent:** AppComponent (global modal)

**Children:**
- FormFieldComponents
- DatePickerComponent
- UserSelectorComponent

**Dependencies:**
- ReactiveFormsModule
- TaskService
- UserService

**Library Components Used:**
- aava-dialog → Modal container
- aava-button → Form action buttons

**TypeScript Specification**

**Inputs:**
- task: Task | null (task to edit, null for new task)
- isOpen: boolean (modal visibility)
- mode: 'create' | 'edit' (modal mode)

**Outputs:**
- taskSaved: EventEmitter<Task> (task save event)
- modalClosed: EventEmitter<void> (modal close event)

**State:**
- taskForm: FormGroup (reactive form)
- isSubmitting: boolean (submission state)
- validationErrors: ValidationError[] (form validation)
- availableUsers: User[] (assignee options)
- availableTags: string[] (tag options)

**Methods:**
- ngOnInit() → Initialize form and load data
- buildForm() → Create reactive form structure
- onSubmit() → Handle form submission
- onCancel() → Close modal without saving
- loadUsers() → Fetch available assignees
- validateForm() → Custom form validation
- resetForm() → Reset form to initial state

**HTML STRUCTURE (PSEUDO CODE ONLY)**

```html
<aava-dialog [isOpen]="isOpen" (closed)="onCancel()" size="large">
  <div class="modal-header">
    <h2>{{ mode === 'create' ? 'Create New Task' : 'Edit Task' }}</h2>
    <aava-button variant="ghost" (click)="onCancel()" aria-label="Close modal">
      <i class="icon-close"></i>
    </aava-button>
  </div>
  
  <form [formGroup]="taskForm" (ngSubmit)="onSubmit()" class="task-form">
    <div class="form-row">
      <div class="form-field">
        <label for="title">Task Title *</label>
        <input id="title" type="text" formControlName="title" 
               [class.error]="taskForm.get('title')?.invalid && taskForm.get('title')?.touched">
        <div class="error-message" *ngIf="taskForm.get('title')?.invalid && taskForm.get('title')?.touched">
          Title is required
        </div>
      </div>
    </div>
    
    <div class="form-row">
      <div class="form-field">
        <label for="description">Description</label>
        <textarea id="description" formControlName="description" rows="3"></textarea>
      </div>
    </div>
    
    <div class="form-row">
      <div class="form-field">
        <label for="priority">Priority</label>
        <select id="priority" formControlName="priority">
          <option value="low">Low</option>
          <option value="medium">Medium</option>
          <option value="high">High</option>
          <option value="urgent">Urgent</option>
        </select>
      </div>
      
      <div class="form-field">
        <label for="status">Status</label>
        <select id="status" formControlName="status">
          <option value="todo">To Do</option>
          <option value="inprogress">In Progress</option>
          <option value="done">Done</option>
        </select>
      </div>
    </div>
    
    <div class="form-row">
      <div class="form-field">
        <label for="assignee">Assignee</label>
        <select id="assignee" formControlName="assigneeId">
          <option value="">Unassigned</option>
          <option *ngFor="let user of availableUsers" [value]="user.id">
            {{ user.name }}
          </option>
        </select>
      </div>
      
      <div class="form-field">
        <label for="dueDate">Due Date</label>
        <input id="dueDate" type="date" formControlName="dueDate">
      </div>
    </div>
    
    <div class="form-row">
      <div class="form-field">
        <label for="tags">Tags</label>
        <input id="tags" type="text" formControlName="tags" 
               placeholder="Enter tags separated by commas">
      </div>
    </div>
    
    <div class="modal-footer">
      <aava-button variant="secondary" type="button" (click)="onCancel()" [disabled]="isSubmitting">
        Cancel
      </aava-button>
      <aava-button variant="primary" type="submit" [disabled]="taskForm.invalid || isSubmitting">
        {{ isSubmitting ? 'Saving...' : (mode === 'create' ? 'Create Task' : 'Update Task') }}
      </aava-button>
    </div>
  </form>
</aava-dialog>
```

**CSS SPECIFICATION**

**Layout:**
- .task-form: flex column with gap
- .form-row: flex row with responsive wrapping
- .form-field: flex column, flex: 1

**Spacing:**
- padding: 1.5rem
- gap: 1rem between form rows
- margin: 0.5rem between label and input

**Component Styles:**
- .modal-header: flex justify-between align-center
- .form-field input, select, textarea: full width, padding: 0.75rem
- .error-message: color: red, font-size: 0.875rem
- .modal-footer: flex justify-end gap: 1rem

**Responsive:**
- Desktop: two-column form layout
- Tablet: single column with full width
- Mobile: stacked form with larger touch targets

**API INTEGRATION**

- onSubmit() → POST /api/tasks (create mode) → Create new task
- onSubmit() → PUT /api/tasks/{id} (edit mode) → Update existing task
- loadUsers() → GET /api/users → Populate assignee dropdown

**ERROR HANDLING**

- API failure → show error message above form
- Validation error → inline field-level error messages
- Network timeout → show retry option

**INTERACTION FLOW**

**User Action → UI → API**
- Open modal → load form data → GET /api/users → populate dropdowns
- Submit form → validate → POST/PUT /api/tasks → close modal and refresh board
- Cancel → close modal → no API calls

### 4.5 HeaderComponent

**Component Type:** Layout

**Mapped From A1:** Top navigation with search and user actions

**Purpose:** Global header with search functionality, notifications, and user profile access

**Parent:** AppComponent

**Children:**
- SearchComponent
- NotificationComponent
- UserMenuComponent

**Dependencies:**
- SearchService
- NotificationService
- AuthService

**Library Components Used:**
- aava-button → Action buttons
- aava-popover → User menu and notifications
- aava-popup → Search suggestions

**TypeScript Specification**

**Inputs:**
- user: User (current user information)
- searchEnabled: boolean (enable search functionality)

**Outputs:**
- searchChanged: EventEmitter<string> (search query changes)
- userMenuClicked: EventEmitter<void> (user menu actions)
- notificationClicked: EventEmitter<Notification> (notification actions)

**State:**
- searchQuery: string (search input value)
- notifications: Notification[] (user notifications)
- showUserMenu: boolean (user menu visibility)
- showNotifications: boolean (notifications panel visibility)
- searchSuggestions: string[] (search autocomplete)

**Methods:**
- onSearchInput(query: string) → Handle search input
- onSearchSubmit() → Submit search query
- toggleUserMenu() → Show/hide user menu
- toggleNotifications() → Show/hide notifications
- onNotificationClick(notification: Notification) → Handle notification click
- loadNotifications() → Fetch user notifications
- logout() → Handle user logout

**HTML STRUCTURE (PSEUDO CODE ONLY)**

```html
<header class="app-header" role="banner">
  <div class="header-brand">
    <img src="/assets/logo.svg" alt="TaskFlow" class="logo">
    <h1 class="brand-name">TaskFlow</h1>
  </div>
  
  <div class="header-search" *ngIf="searchEnabled">
    <div class="search-container">
      <input type="search" 
             [(ngModel)]="searchQuery"
             (input)="onSearchInput($event.target.value)"
             (keyup.enter)="onSearchSubmit()"
             placeholder="Search tasks, users, or projects..."
             aria-label="Search"
             class="search-input">
      <aava-button variant="ghost" (click)="onSearchSubmit()" aria-label="Submit search">
        <i class="icon-search"></i>
      </aava-button>
      
      <aava-popup [isOpen]="searchSuggestions.length > 0" position="bottom-start">
        <ul class="search-suggestions">
          <li *ngFor="let suggestion of searchSuggestions" 
              (click)="selectSuggestion(suggestion)"
              class="suggestion-item">
            {{ suggestion }}
          </li>
        </ul>
      </aava-popup>
    </div>
  </div>
  
  <div class="header-actions">
    <div class="notification-container">
      <aava-button variant="ghost" 
                   (click)="toggleNotifications()"
                   [class.has-notifications]="notifications.length > 0"
                   aria-label="Notifications">
        <i class="icon-bell"></i>
        <span class="notification-badge" *ngIf="notifications.length > 0">
          {{ notifications.length }}
        </span>
      </aava-button>
      
      <aava-popover [isOpen]="showNotifications" (closed)="showNotifications = false" position="bottom-end">
        <div class="notifications-panel">
          <h3>Notifications</h3>
          <div class="notification-list" *ngIf="notifications.length > 0">
            <div *ngFor="let notification of notifications" 
                 class="notification-item"
                 (click)="onNotificationClick(notification)">
              <div class="notification-content">
                <h4>{{ notification.title }}</h4>
                <p>{{ notification.message }}</p>
                <span class="notification-time">{{ notification.createdAt | date:'short' }}</span>
              </div>
            </div>
          </div>
          <div class="empty-notifications" *ngIf="notifications.length === 0">
            <p>No new notifications</p>
          </div>
        </div>
      </aava-popover>
    </div>
    
    <aava-button variant="ghost" aria-label="Settings">
      <i class="icon-settings"></i>
    </aava-button>
    
    <div class="user-menu-container">
      <aava-button variant="ghost" (click)="toggleUserMenu()" class="user-button">
        <img [src]="user.avatar" [alt]="user.name" class="user-avatar">
        <span class="user-name">{{ user.name }}</span>
        <i class="icon-chevron-down"></i>
      </aava-button>
      
      <aava-popover [isOpen]="showUserMenu" (closed)="showUserMenu = false" position="bottom-end">
        <div class="user-menu">
          <div class="user-info">
            <img [src]="user.avatar" [alt]="user.name" class="user-avatar-large">
            <div class="user-details">
              <h4>{{ user.name }}</h4>
              <p>{{ user.email }}</p>
            </div>
          </div>
          <hr>
          <button class="menu-item" (click)="viewProfile()">
            <i class="icon-user"></i> Profile
          </button>
          <button class="menu-item" (click)="openSettings()">
            <i class="icon-settings"></i> Settings
          </button>
          <hr>
          <button class="menu-item" (click)="logout()">
            <i class="icon-logout"></i> Logout
          </button>
        </div>
      </aava-popover>
    </div>
  </div>
</header>
```

**CSS SPECIFICATION**

**Layout:**
- .app-header: flex justify-between align-center, height: 64px
- .header-search: flex-grow: 1, max-width: 600px
- .header-actions: flex align-center gap: 0.5rem

**Spacing:**
- padding: 0 1.5rem
- gap: 1rem between major sections

**Component Styles:**
- .logo: height: 32px, width: auto
- .search-input: padding: 0.5rem 1rem, border-radius: 24px
- .user-avatar: width: 32px, height: 32px, border-radius: 50%
- .notification-badge: position: absolute, top: -4px, right: -4px

**Responsive:**
- Desktop: full header with all elements
- Tablet: hide user name, compact search
- Mobile: hide search, show only essential actions

**API INTEGRATION**

- onSearchInput() → GET /api/search/suggestions → Populate search suggestions
- onSearchSubmit() → GET /api/search → Execute search query
- loadNotifications() → GET /api/notifications → Fetch user notifications
- logout() → POST /api/auth/logout → Handle user logout

**ERROR HANDLING**

- Search API failure → show "Search unavailable" message
- Notification load failure → show error in notifications panel
- Logout failure → show error message and retry option

**INTERACTION FLOW**

**User Action → UI → API**
- Type in search → debounce input → GET /api/search/suggestions → show dropdown
- Click notification → mark as read → PUT /api/notifications/{id} → update UI
- Click logout → confirm action → POST /api/auth/logout → redirect to login

### 4.6 SidebarComponent

**Component Type:** Layout

**Mapped From A1:** Left navigation with workflow scenarios and quick actions

**Purpose:** Primary navigation sidebar with brand identity, workflow scenarios, and quick actions

**Parent:** AppComponent

**Children:**
- NavigationMenuComponent
- QuickActionsComponent

**Dependencies:**
- Router
- NavigationService

**Library Components Used:**
- aava-button → Navigation and action buttons

**TypeScript Specification**

**Inputs:**
- currentView: string (active view identifier)
- collapsed: boolean (sidebar collapse state)

**Outputs:**
- viewChanged: EventEmitter<string> (view navigation)
- quickActionClicked: EventEmitter<string> (quick action events)
- sidebarToggled: EventEmitter<boolean> (collapse toggle)

**State:**
- activeMenuItem: string (currently selected menu item)
- isCollapsed: boolean (collapse state)
- workflowScenarios: MenuItem[] (navigation menu items)
- quickActions: QuickAction[] (quick action items)

**Methods:**
- onMenuItemClick(item: string) → Handle navigation
- onQuickActionClick(action: string) → Handle quick actions
- toggleSidebar() → Toggle collapse state
- isActiveRoute(route: string) → Check if route is active

**HTML STRUCTURE (PSEUDO CODE ONLY)**

```html
<aside class="sidebar" 
       [class.collapsed]="isCollapsed"
       role="navigation"
       aria-label="Main navigation">
  
  <div class="sidebar-header">
    <div class="brand-section">
      <img src="/assets/logo.svg" alt="TaskFlow" class="brand-logo">
      <span class="brand-name" *ngIf="!isCollapsed">TaskFlow</span>
    </div>
    <aava-button variant="ghost" 
                 (click)="toggleSidebar()"
                 [attr.aria-label]="isCollapsed ? 'Expand sidebar' : 'Collapse sidebar'"
                 class="toggle-button">
      <i [class]="isCollapsed ? 'icon-menu' : 'icon-close'"></i>
    </aava-button>
  </div>
  
  <nav class="sidebar-nav">
    <div class="nav-section">
      <h3 class="nav-heading" *ngIf="!isCollapsed">Workflow Scenarios</h3>
      <ul class="nav-menu" role="menubar">
        <li *ngFor="let item of workflowScenarios" class="nav-item" role="none">
          <aava-button variant="ghost" 
                       [class.active]="isActiveRoute(item.route)"
                       (click)="onMenuItemClick(item.route)"
                       class="nav-link"
                       role="menuitem"
                       [attr.aria-current]="isActiveRoute(item.route) ? 'page' : null">
            <i [class]="item.icon"></i>
            <span class="nav-text" *ngIf="!isCollapsed">{{ item.label }}</span>
          </aava-button>
        </li>
      </ul>
    </div>
    
    <div class="nav-section">
      <h3 class="nav-heading" *ngIf="!isCollapsed">Quick Actions</h3>
      <ul class="quick-actions" role="menubar">
        <li *ngFor="let action of quickActions" class="action-item" role="none">
          <aava-button variant="secondary" 
                       (click)="onQuickActionClick(action.id)"
                       class="action-button"
                       role="menuitem"
                       [attr.title]="isCollapsed ? action.label : null">
            <i [class]="action.icon"></i>
            <span class="action-text" *ngIf="!isCollapsed">{{ action.label }}</span>
          </aava-button>
        </li>
      </ul>
    </div>
  </nav>
  
  <div class="sidebar-footer" *ngIf="!isCollapsed">
    <div class="footer-content">
      <p class="version-info">Version 2.1.0</p>
      <aava-button variant="ghost" size="small" (click)="openHelp()">
        <i class="icon-help"></i> Help
      </aava-button>
    </div>
  </div>
</aside>
```

**CSS SPECIFICATION**

**Layout:**
- .sidebar: flex column, height: 100vh, width: 280px (expanded), 64px (collapsed)
- .sidebar-nav: flex-grow: 1, overflow-y: auto
- .nav-menu: flex column with no gaps

**Spacing:**
- padding: 1rem (expanded), 0.5rem (collapsed)
- gap: 0.5rem between nav sections

**Component Styles:**
- .brand-logo: height: 32px, width: 32px
- .nav-link: width: 100%, justify-content: flex-start, padding: 0.75rem
- .nav-link.active: background-color: primary-light, border-left: 3px solid primary
- .collapsed .nav-text, .collapsed .action-text: display: none

**Responsive:**
- Desktop: full sidebar with labels
- Tablet: auto-collapse to icon-only mode
- Mobile: overlay sidebar that can be toggled

**API INTEGRATION**

- No direct API calls (navigation only)
- Route changes handled by Angular Router

**ERROR HANDLING**

- Navigation failure → show error toast
- Route not found → redirect to default view
- Permission denied → disable restricted menu items

**INTERACTION FLOW**

**User Action → UI → API**
- Click menu item → emit viewChanged → parent handles routing
- Click quick action → emit quickActionClicked → parent handles action
- Toggle sidebar → emit sidebarToggled → parent updates layout

## 5. COMPONENT MAPPING SUMMARY

| A1 Component | Final Component | Type |
|--------------|----------------|---------|
| KanbanBoardComponent | KanbanBoardComponent | Feature |
| KanbanColumnComponent | KanbanColumnComponent | Feature |
| TaskCardComponent | TaskCardComponent | Shared |
| TaskModalComponent | TaskModalComponent | Shared |
| HeaderComponent | HeaderComponent | Layout |
| SidebarComponent | SidebarComponent | Layout |
| Button | aava-button | Library |
| Dialog | aava-dialog | Library |
| Popup | aava-popup | Library |
| Popover | aava-popover | Library |

## 6. DATA FLOW (FROM AGENT-1)

**Parent → Child:**

**AppComponent →**
- HeaderComponent (user: User, searchEnabled: boolean)
- SidebarComponent (currentView: string, collapsed: boolean)
- KanbanBoardComponent (loading: boolean, error: string | null)

**KanbanBoardComponent →**
- KanbanColumnComponent[] (column: KanbanColumn, allowDrop: boolean, readonly: boolean)

**KanbanColumnComponent →**
- TaskCardComponent[] (task: Task, draggable: boolean, selectable: boolean)

**Global Modal →**
- TaskModalComponent (task: Task | null, isOpen: boolean, mode: 'create' | 'edit')

## 7. API USAGE SUMMARY

**Component → API:**

- **KanbanBoardComponent** → GET /api/youth-accounts/{id}/dashboard (load initial kanban data)
- **KanbanBoardComponent** → PUT /api/tasks/{id}/status (update task status on drag/drop)
- **TaskModalComponent** → POST /api/tasks (create new task)
- **TaskModalComponent** → PUT /api/tasks/{id} (update existing task)
- **TaskModalComponent** → GET /api/users (load assignee options)
- **HeaderComponent** → GET /api/search/suggestions (search autocomplete)
- **HeaderComponent** → GET /api/search (execute search)
- **HeaderComponent** → GET /api/notifications (load user notifications)
- **HeaderComponent** → POST /api/auth/logout (user logout)

**Data Transformation:**
- Dashboard API response → KanbanColumn[] format with financial tasks
- Fund transfer operations → Task objects for kanban display
- Transaction history → Task detail information
- User data → Assignee dropdown options
- Search results → Task filtering and highlighting

## 8. NOTES & CONSTRAINTS

**Implementation Rules:**
- HTML must follow A1 structure strictly - three-column CSS Grid layout is mandatory
- Do NOT create new components unnecessarily - reuse library components where possible
- Prefer aava-button, aava-dialog, aava-popup, aava-popover from uicomplibraryplay
- HTML must be pseudo-code ONLY (NOT real framework code)
- CSS should include only key classes and layout rules, not full stylesheets
- All components must implement proper ARIA attributes for accessibility
- Responsive breakpoints: desktop (>1024px), tablet (768px-1024px), mobile (<768px)
- Error handling must be implemented at component level with user-friendly messages
- Form validation must be client-side with server-side backup
- Drag and drop functionality must use Angular CDK
- All API calls must include proper error handling and loading states

**Architecture Constraints:**
- Follow Angular component lifecycle hooks (OnInit, OnDestroy)
- Use reactive forms for all form inputs
- Implement proper TypeScript interfaces for all data models
- Use EventEmitter for component communication
- Follow single responsibility principle for each component
- Implement proper change detection strategies for performance
- Use trackBy functions for *ngFor loops with dynamic data
- Implement proper memory management and subscription cleanup

**Accessibility Requirements:**
- All interactive elements must be keyboard accessible
- Proper ARIA labels and roles for screen readers
- Color contrast ratios must meet WCAG 2.1 AA standards
- Focus management for modal dialogs and dynamic content
- Alternative text for all images and icons
- Semantic HTML structure with proper heading hierarchy

**Performance Considerations:**
- Implement OnPush change detection where appropriate
- Use lazy loading for feature modules
- Optimize bundle size with tree shaking
- Implement virtual scrolling for large task lists
- Use proper caching strategies for API responses
- Minimize DOM manipulations and use Angular's built-in optimizations