## UI COMPONENT SPECIFICATIONS

1. INPUT REFERENCE (FROM AGENT-1)

Page Name: [MISSING - Agent-1 output not provided]
Mapped Layout: [MISSING - Agent-1 output not provided]
Components from A1:
- [MISSING - Agent-1 output not provided]
- [MISSING - Agent-1 output not provided]

NOTE: Agent-1 output not found. This specification is generated as a template.

2. GLOBAL ARCHITECTURE SUMMARY

Layout Type: Mixed (Grid + Flex)
Main Sections:
- Header
- Sidebar
- Main Content
- Footer
- Modals

Component Strategy: Reuse existing UI library components where possible

Reused Components:
- Button (from UI library)
- Input (from UI library)
- Card (from UI library)
- Modal (from UI library)

New Components (if any):
- DashboardLayout
- FeatureCard

3. FOLDER STRUCTURE

src/
 ├── pages/
 │    └── dashboard/
 │         ├── DashboardPage.tsx
 │
 ├── components/
 │    ├── layout/
 │    │    ├── HeaderComponent.tsx
 │    │    ├── SidebarComponent.tsx
 │    │    └── DashboardLayout.tsx
 │    ├── feature/
 │    │    ├── AccountCard.tsx
 │    │    ├── TransactionList.tsx
 │    │    └── AddFundsModal.tsx
 │    ├── shared/
 │    │    ├── LoadingSpinner.tsx
 │    │    └── ErrorMessage.tsx

4. COMPONENT IMPLEMENTATION

### 4.1 DashboardPage

Component Type: Page
Mapped From A1: [Main page container]
Purpose: Main dashboard page that orchestrates all dashboard components
Parent: App
Children:
- HeaderComponent
- SidebarComponent
- DashboardLayout

Dependencies:
- React
- UI Component Library

Library Components Used:
- Container → Layout wrapper
- Grid → Main layout structure

TypeScript Specification

Inputs:
- userId: string
- userRole: string

Outputs:
- onNavigate(route: string)
- onLogout()

State:
- isLoading: boolean
- error: string | null
- dashboardData: DashboardData

Methods:
- loadDashboardData() → Promise<void>
- handleNavigation(route: string) → void
- handleLogout() → void

HTML STRUCTURE (PSEUDO CODE ONLY)

<Container>
  <HeaderComponent 
    user={user}
    onLogout={handleLogout}
  />
  <Grid container>
    <Grid item sidebar>
      <SidebarComponent 
        activeRoute={activeRoute}
        onNavigate={handleNavigation}
      />
    </Grid>
    <Grid item main>
      <DashboardLayout 
        data={dashboardData}
        loading={isLoading}
        error={error}
      />
    </Grid>
  </Grid>
</Container>

CSS SPECIFICATION

Layout:
- display: grid
- grid-template-areas: "header header" "sidebar main"
- grid-template-rows: auto 1fr
- grid-template-columns: 250px 1fr

Spacing:
- padding: 0
- gap: 0

Component Styles:
- .dashboard-container: full height viewport
- .main-content: overflow-y auto

Responsive:
- @media (max-width: 768px): collapse sidebar
- @media (max-width: 480px): stack layout

API INTEGRATION

- loadDashboardData() → GET /api/dashboard → fetch user dashboard data
- handleLogout() → POST /api/auth/logout → logout user session

ERROR HANDLING

- API failure → show error snackbar
- Network error → show retry button
- Authentication error → redirect to login

INTERACTION FLOW

User Action → UI → API
- Page load → show loading → fetch dashboard data → display content
- Logout click → confirm modal → logout API → redirect to login

### 4.2 HeaderComponent

Component Type: Layout
Mapped From A1: [Header section]
Purpose: Application header with navigation and user actions
Parent: DashboardPage
Children:
- UserMenu
- NotificationBell

Dependencies:
- UI Component Library
- Icons library

Library Components Used:
- AppBar → Header container
- Toolbar → Header content layout
- IconButton → Action buttons

TypeScript Specification

Inputs:
- user: UserProfile
- notifications: Notification[]

Outputs:
- onLogout()
- onProfileClick()
- onNotificationClick(id: string)

State:
- menuOpen: boolean
- notificationCount: number

Methods:
- toggleMenu() → void
- handleNotificationClick(id: string) → void

HTML STRUCTURE (PSEUDO CODE ONLY)

<AppBar position="fixed">
  <Toolbar>
    <Logo />
    <Spacer />
    <NotificationBell 
      count={notificationCount}
      onClick={handleNotificationClick}
    />
    <UserMenu 
      user={user}
      open={menuOpen}
      onToggle={toggleMenu}
      onLogout={onLogout}
    />
  </Toolbar>
</AppBar>

CSS SPECIFICATION

Layout:
- position: fixed
- top: 0
- width: 100%
- z-index: 1000

Spacing:
- height: 64px
- padding: 0 24px

Component Styles:
- .header-toolbar: justify-content space-between
- .user-section: display flex, align-items center

Responsive:
- @media (max-width: 768px): reduce padding to 16px

API INTEGRATION

- fetchNotifications() → GET /api/notifications → get user notifications

ERROR HANDLING

- Notification fetch error → show fallback count
- Logout error → show error message

INTERACTION FLOW

User Action → UI → API
- Notification click → mark as read → update count
- Profile click → open user menu → show options

### 4.3 AccountCard

Component Type: Feature
Mapped From A1: [Account display card]
Purpose: Display account information and quick actions
Parent: DashboardLayout
Children:
- QuickActionButtons

Dependencies:
- UI Component Library
- Currency formatter

Library Components Used:
- Card → Container
- CardContent → Content wrapper
- Button → Action buttons

TypeScript Specification

Inputs:
- account: AccountData
- loading: boolean

Outputs:
- onTransfer()
- onAddFunds()
- onViewDetails(accountId: string)

State:
- expanded: boolean

Methods:
- toggleExpanded() → void
- formatCurrency(amount: number) → string

HTML STRUCTURE (PSEUDO CODE ONLY)

<Card elevation={2}>
  <CardContent>
    <AccountHeader 
      name={account.name}
      number={account.number}
      balance={account.balance}
    />
    <QuickActions>
      <Button onClick={onTransfer}>Transfer</Button>
      <Button onClick={onAddFunds}>Add Funds</Button>
      <Button onClick={() => onViewDetails(account.id)}>Details</Button>
    </QuickActions>
  </CardContent>
</Card>

CSS SPECIFICATION

Layout:
- display: flex
- flex-direction: column
- min-height: 200px

Spacing:
- padding: 24px
- margin-bottom: 16px

Component Styles:
- .account-header: margin-bottom 16px
- .balance-amount: font-size 24px, font-weight bold
- .quick-actions: display flex, gap 8px

Responsive:
- @media (max-width: 768px): stack buttons vertically

API INTEGRATION

- refreshBalance() → GET /api/accounts/{id}/balance → update account balance

ERROR HANDLING

- Balance fetch error → show last known balance with warning
- Action button error → disable button and show tooltip

INTERACTION FLOW

User Action → UI → API
- Transfer click → open transfer modal → validate → submit transfer
- Add funds click → open add funds modal → process payment

### 4.4 AddFundsModal

Component Type: Feature
Mapped From A1: [Add funds dialog]
Purpose: Modal for adding funds to account
Parent: DashboardLayout
Children:
- PaymentForm
- AmountInput

Dependencies:
- UI Component Library
- Form validation library

Library Components Used:
- Modal → Container
- TextField → Input fields
- Button → Action buttons

TypeScript Specification

Inputs:
- open: boolean
- accountId: string

Outputs:
- onClose()
- onSuccess(amount: number)

State:
- amount: number
- paymentMethod: string
- loading: boolean
- errors: FormErrors

Methods:
- validateAmount(amount: number) → boolean
- handleSubmit() → Promise<void>
- resetForm() → void

HTML STRUCTURE (PSEUDO CODE ONLY)

<Modal open={open} onClose={onClose}>
  <ModalContent>
    <ModalHeader>
      <Title>Add Funds</Title>
      <CloseButton onClick={onClose} />
    </ModalHeader>
    <ModalBody>
      <AmountInput 
        value={amount}
        onChange={setAmount}
        error={errors.amount}
      />
      <PaymentMethodSelect 
        value={paymentMethod}
        onChange={setPaymentMethod}
      />
    </ModalBody>
    <ModalFooter>
      <Button variant="outlined" onClick={onClose}>Cancel</Button>
      <Button variant="contained" onClick={handleSubmit} loading={loading}>
        Add Funds
      </Button>
    </ModalFooter>
  </ModalContent>
</Modal>

CSS SPECIFICATION

Layout:
- position: fixed
- top: 50%
- left: 50%
- transform: translate(-50%, -50%)

Spacing:
- width: 400px
- padding: 24px

Component Styles:
- .modal-content: background white, border-radius 8px
- .modal-body: padding 16px 0
- .form-field: margin-bottom 16px

Responsive:
- @media (max-width: 480px): width 90vw, padding 16px

API INTEGRATION

- submitAddFunds() → POST /api/accounts/{id}/add-funds → process fund addition
- validatePayment() → POST /api/payments/validate → validate payment method

ERROR HANDLING

- Validation error → show inline field errors
- API error → show error message in modal
- Network error → show retry option

INTERACTION FLOW

User Action → UI → API
- Amount input → validate format → show/hide errors
- Submit click → validate form → show loading → API call → success/error

### 4.5 SidebarComponent

Component Type: Layout
Mapped From A1: [Navigation sidebar]
Purpose: Main navigation component with menu items
Parent: DashboardPage
Children:
- NavigationMenu
- UserProfile

Dependencies:
- UI Component Library
- Router library

Library Components Used:
- Drawer → Sidebar container
- List → Navigation list
- ListItem → Menu items

TypeScript Specification

Inputs:
- activeRoute: string
- collapsed: boolean
- menuItems: MenuItem[]

Outputs:
- onNavigate(route: string)
- onToggleCollapse()

State:
- hoveredItem: string | null

Methods:
- handleItemClick(route: string) → void
- handleItemHover(itemId: string) → void

HTML STRUCTURE (PSEUDO CODE ONLY)

<Drawer variant="permanent" collapsed={collapsed}>
  <SidebarHeader>
    <Logo collapsed={collapsed} />
    <CollapseButton onClick={onToggleCollapse} />
  </SidebarHeader>
  <NavigationList>
    {menuItems.map(item => (
      <ListItem 
        key={item.id}
        active={activeRoute === item.route}
        onClick={() => onNavigate(item.route)}
        onHover={() => handleItemHover(item.id)}
      >
        <ItemIcon icon={item.icon} />
        <ItemText text={item.label} collapsed={collapsed} />
      </ListItem>
    ))}
  </NavigationList>
</Drawer>

CSS SPECIFICATION

Layout:
- position: fixed
- left: 0
- top: 64px
- height: calc(100vh - 64px)
- width: 250px (expanded) / 60px (collapsed)

Spacing:
- padding: 16px 0

Component Styles:
- .sidebar-header: padding 16px, border-bottom 1px solid
- .nav-item: padding 12px 16px, cursor pointer
- .nav-item.active: background-color primary
- .nav-item:hover: background-color hover

Responsive:
- @media (max-width: 768px): transform translateX(-100%) when collapsed

API INTEGRATION

- fetchMenuItems() → GET /api/navigation/menu → get user-specific menu items

ERROR HANDLING

- Menu fetch error → show default menu items
- Navigation error → show error toast

INTERACTION FLOW

User Action → UI → API
- Menu item click → highlight active → navigate to route
- Collapse toggle → animate width → save preference

### 4.6 TransactionList

Component Type: Feature
Mapped From A1: [Transaction history table]
Purpose: Display list of recent transactions with filtering
Parent: DashboardLayout
Children:
- TransactionItem
- FilterControls

Dependencies:
- UI Component Library
- Date formatting library

Library Components Used:
- Table → Transaction table
- TableRow → Transaction rows
- Pagination → Page controls

TypeScript Specification

Inputs:
- transactions: Transaction[]
- loading: boolean
- pageSize: number

Outputs:
- onPageChange(page: number)
- onFilterChange(filters: TransactionFilters)
- onTransactionClick(transactionId: string)

State:
- currentPage: number
- filters: TransactionFilters
- sortBy: string
- sortOrder: 'asc' | 'desc'

Methods:
- handleSort(column: string) → void
- formatDate(date: Date) → string
- formatAmount(amount: number) → string

HTML STRUCTURE (PSEUDO CODE ONLY)

<Card>
  <CardHeader>
    <Title>Recent Transactions</Title>
    <FilterControls 
      filters={filters}
      onChange={onFilterChange}
    />
  </CardHeader>
  <CardContent>
    <Table>
      <TableHeader>
        <TableRow>
          <TableCell sortable onClick={() => handleSort('date')}>Date</TableCell>
          <TableCell sortable onClick={() => handleSort('description')}>Description</TableCell>
          <TableCell sortable onClick={() => handleSort('amount')}>Amount</TableCell>
          <TableCell>Status</TableCell>
        </TableRow>
      </TableHeader>
      <TableBody>
        {transactions.map(transaction => (
          <TransactionItem 
            key={transaction.id}
            transaction={transaction}
            onClick={() => onTransactionClick(transaction.id)}
          />
        ))}
      </TableBody>
    </Table>
    <Pagination 
      page={currentPage}
      total={totalPages}
      onChange={onPageChange}
    />
  </CardContent>
</Card>

CSS SPECIFICATION

Layout:
- display: flex
- flex-direction: column
- min-height: 400px

Spacing:
- padding: 0
- margin-bottom: 24px

Component Styles:
- .transaction-row: cursor pointer, hover background
- .amount-positive: color green
- .amount-negative: color red
- .status-pending: color orange

Responsive:
- @media (max-width: 768px): hide non-essential columns
- @media (max-width: 480px): stack transaction info vertically

API INTEGRATION

- fetchTransactions() → GET /api/transactions → get transaction list
- fetchTransactionDetails() → GET /api/transactions/{id} → get transaction details

ERROR HANDLING

- Transaction fetch error → show error message with retry
- Empty state → show "No transactions found" message

INTERACTION FLOW

User Action → UI → API
- Filter change → update filters → fetch filtered transactions
- Sort click → update sort order → re-sort data
- Transaction click → show transaction details modal

5. COMPONENT MAPPING SUMMARY

| A1 Component | Final Component | Type |
|--------------|----------------|---------|
| Page Container | DashboardPage | Page |
| Header | HeaderComponent | Layout |
| Sidebar | SidebarComponent | Layout |
| Main Layout | DashboardLayout | Layout |
| Account Card | AccountCard | Feature |
| Transaction List | TransactionList | Feature |
| Add Funds Dialog | AddFundsModal | Feature |
| Loading Spinner | LoadingSpinner | Shared |
| Error Message | ErrorMessage | Shared |

6. DATA FLOW (FROM AGENT-1)

Parent → Child:
DashboardPage → HeaderComponent (user, notifications)
DashboardPage → SidebarComponent (activeRoute, menuItems, collapsed)
DashboardPage → DashboardLayout (dashboardData, loading, error)
DashboardLayout → AccountCard (account, loading)
DashboardLayout → TransactionList (transactions, loading, pageSize)
DashboardLayout → AddFundsModal (open, accountId)

Child → Parent:
HeaderComponent → DashboardPage (onLogout, onNavigate)
SidebarComponent → DashboardPage (onNavigate, onToggleCollapse)
AccountCard → DashboardLayout (onTransfer, onAddFunds, onViewDetails)
TransactionList → DashboardLayout (onPageChange, onFilterChange)
AddFundsModal → DashboardLayout (onClose, onSuccess)

7. API USAGE SUMMARY

Component → API:
- DashboardPage → GET /api/dashboard (dashboard data)
- DashboardPage → POST /api/auth/logout (logout)
- HeaderComponent → GET /api/notifications (notifications)
- SidebarComponent → GET /api/navigation/menu (menu items)
- AccountCard → GET /api/accounts/{id}/balance (account balance)
- TransactionList → GET /api/transactions (transaction list)
- TransactionList → GET /api/transactions/{id} (transaction details)
- AddFundsModal → POST /api/accounts/{id}/add-funds (add funds)
- AddFundsModal → POST /api/payments/validate (validate payment)

8. NOTES & CONSTRAINTS

- HTML must follow A1 structure strictly (A1 output not available in this case)
- Do NOT create new components unnecessarily - reuse UI library components
- Prefer reusable library components over custom implementations
- HTML must be pseudo-code ONLY (NOT real framework code)
- CSS should include only key classes and layout rules, not full stylesheets
- All components must handle loading and error states appropriately
- Responsive design is mandatory for all components
- API integration must include proper error handling and retry mechanisms
- Form validation must be client-side first, then server-side
- All user interactions must provide immediate feedback
- Components should be designed for reusability across different pages
- State management should follow React best practices
- Accessibility (a11y) requirements must be considered in implementation
- Performance optimization through lazy loading and memoization where appropriate

IMPORTANT: This specification was generated without Agent-1 output. In a real implementation, the HTML Structure and UI Components Architecture from Agent-1 must be used as the primary source of truth for accurate component specifications.