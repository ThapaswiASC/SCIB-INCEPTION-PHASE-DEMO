## UI COMPONENT SPECIFICATIONS

1. INPUT REFERENCE (FROM AGENT-1)

Page Name: Task Management Dashboard with Task Creation Modal

Mapped Layout: Flex-based layout with sidebar navigation and main content area

Components from A1:
- TaskCreationComponent (Primary Focus - NEW)
- TaskFormComponent
- ValidationFeedbackComponent
- CharacterCounterComponent
- LoadingSpinnerComponent
- KanbanBoardComponent
- HeaderComponent
- SidebarComponent
- ButtonComponent (from library)
- ModalComponent

2. GLOBAL ARCHITECTURE SUMMARY

Layout Type: Flex-based layout with sidebar and main content, Grid layouts for cards, Modal overlays with backdrop blur

Main Sections:
- Header (Search, Notifications, User Actions)
- Sidebar (Brand, Navigation Menu, Footer)
- Main Content (Kanban Board, Task Creation Modal)
- Modals (Task Creation, Team Communication, Column Configuration)

Component Strategy: Reuse existing library components where possible, create new components only for specific task creation requirements

Reused Components:
- ButtonComponent (from uicomplibraryplay library)
- ModalComponent (existing)
- CardComponent (existing)
- BadgeComponent (existing)
- InputComponent (existing)

New Components (if any):
- TaskCreationComponent (main component)
- TaskFormComponent (reactive form)
- ValidationFeedbackComponent (real-time validation)
- CharacterCounterComponent (character counting)
- LoadingSpinnerComponent (loading states)

3. FOLDER STRUCTURE

src/
 ├── app/
 │    ├── components/
 │    │   ├── task-creation/
 │    │   │   ├── task-creation.component.ts
 │    │   │   ├── task-creation.component.html
 │    │   │   ├── task-creation.component.scss
 │    │   │   ├── task-creation.component.spec.ts
 │    │   │   ├── task-form/
 │    │   │   ├── validation-feedback/
 │    │   │   ├── character-counter/
 │    │   │   └── loading-spinner/
 │    │   ├── layout/
 │    │   │   ├── header/
 │    │   │   ├── sidebar/
 │    │   │   └── main-container/
 │    │   ├── kanban/
 │    │   │   ├── kanban-board/
 │    │   │   ├── kanban-column/
 │    │   │   └── task-card/
 │    │   └── shared/
 │    │       ├── button/
 │    │       ├── modal/
 │    │       ├── card/
 │    │       ├── badge/
 │    │       └── input/
 │    ├── services/
 │    │   ├── task.service.ts
 │    │   ├── auth.service.ts
 │    │   └── api.service.ts
 │    ├── models/
 │    │   ├── task.model.ts
 │    │   ├── user.model.ts
 │    │   └── api-response.model.ts
 │    ├── validators/
 │    │   ├── custom-validators.ts
 │    │   └── form-validators.ts
 │    └── constants/
 │        ├── api-endpoints.ts
 │        └── validation-messages.ts
 └── assets/
     ├── styles/
     └── icons/

4. COMPONENT IMPLEMENTATION

### 4.1 TaskCreationComponent

Component Type: Feature

Mapped From A1: TaskCreationComponent (Primary Focus - NEW)

Purpose: Main component for creating new tasks with comprehensive form validation, reactive forms, and API integration

Parent: App Container / Kanban Board

Children:
- TaskFormComponent
- ValidationFeedbackComponent
- CharacterCounterComponent
- LoadingSpinnerComponent

Dependencies:
- Angular Reactive Forms
- TaskService
- FormBuilder
- Validators

Library Components Used:
- ButtonComponent → Primary/Secondary variants for form actions
- ModalComponent → Overlay container

TypeScript Specification

Inputs:
- isOpen: boolean (modal visibility state)
- onClose: () => void (modal close handler)
- onTaskCreated: (task: Task) => void (success callback)

Outputs:
- taskCreated: EventEmitter<Task> (emits created task)
- formClosed: EventEmitter<void> (emits when modal closes)
- validationChanged: EventEmitter<boolean> (emits form validity state)

State:
- formGroup: FormGroup (reactive form instance)
- isLoading: boolean (API call state)
- validationErrors: ValidationError[] (form validation errors)
- characterCounts: { title: number, description: number } (character counters)
- submitAttempted: boolean (tracks submission attempts)

Methods:
- initializeForm() → void (setup reactive form with validators)
- onSubmit() → void (handle form submission)
- resetForm() → void (reset form after successful submission)
- closeModal() → void (close modal and cleanup)
- updateCharacterCount(field: string, count: number) → void (update character counters)

HTML STRUCTURE (PSEUDO CODE ONLY)

<modal-component [isOpen]="isOpen" (onClose)="closeModal()">
  <div class="task-creation-container">
    <div class="modal-header">
      <h2>Create New Task</h2>
      <button-component variant="secondary" (userClick)="closeModal()">Close</button-component>
    </div>
    
    <div class="modal-body">
      <task-form-component 
        [formGroup]="formGroup" 
        [isLoading]="isLoading"
        (formSubmit)="onSubmit()"
        (characterCountChanged)="updateCharacterCount($event.field, $event.count)">
      </task-form-component>
      
      <validation-feedback-component 
        [field]="formGroup.get('title')"
        fieldName="title"
        [validationRules]="titleValidationRules">
      </validation-feedback-component>
      
      <character-counter-component 
        [currentLength]="characterCounts.title"
        [maxLength]="255"
        fieldName="title">
      </character-counter-component>
    </div>
    
    <div class="modal-footer">
      <button-component 
        variant="secondary" 
        (userClick)="closeModal()"
        [disabled]="isLoading">Cancel</button-component>
      <button-component 
        variant="primary" 
        (userClick)="onSubmit()"
        [processing]="isLoading"
        [disabled]="!formGroup.valid">Create Task</button-component>
    </div>
    
    <loading-spinner-component 
      [isVisible]="isLoading"
      message="Creating task...">
    </loading-spinner-component>
  </div>
</modal-component>

CSS SPECIFICATION

Layout:
- display: flex, flex-direction: column
- modal-container: max-width 600px, centered
- form sections: gap 1.5rem

Spacing:
- padding: 2rem
- gap: 1rem between form fields
- margin: 0.5rem for validation messages

Component Styles:
- .task-creation-container: background glass morphism
- .modal-header: border-bottom, flex justify-between
- .modal-body: flex-1, overflow-y auto
- .modal-footer: border-top, flex justify-end gap 1rem

Responsive:
- @media (max-width: 768px): full-width modal
- @media (max-width: 480px): reduced padding

API INTEGRATION

- onSubmit() → POST /api/v1/tasks → Create new task with form data
- TaskService.createTask(taskData) → Handle API response and errors

ERROR HANDLING

- API failure → Display error snackbar with retry option
- Validation error → Show inline validation messages
- Network error → Show connection error message
- 401 Unauthorized → Redirect to login
- 403 Forbidden → Show permission error
- 429 Rate Limit → Show rate limit message with retry timer

INTERACTION FLOW

User Action → UI → API
- Click "Add Task" → Open modal → Initialize form → No API call
- Type in form fields → Real-time validation → Update character counters → No API call
- Click "Create Task" → Validate form → Show loading → POST /api/v1/tasks → Handle response
- Success response → Hide loading → Show success message → Reset form → Close modal → Emit taskCreated
- Error response → Hide loading → Show error message → Keep modal open → Focus error field

### 4.2 TaskFormComponent

Component Type: Feature

Mapped From A1: TaskFormComponent

Purpose: Reactive form implementation with comprehensive validation for task creation

Parent: TaskCreationComponent

Children:
- ValidationFeedbackComponent (for each field)
- CharacterCounterComponent (for text fields)

Dependencies:
- Angular Reactive Forms
- Custom Validators
- FormBuilder

Library Components Used:
- InputComponent → Text inputs, textareas, selects
- ButtonComponent → Form action buttons

TypeScript Specification

Inputs:
- formGroup: FormGroup (reactive form instance)
- isLoading: boolean (submission state)

Outputs:
- formSubmit: EventEmitter<any> (emits form data on submit)
- characterCountChanged: EventEmitter<{field: string, count: number}> (character count updates)

State:
- titleCharCount: number (title character count)
- descriptionCharCount: number (description character count)
- priorityOptions: PriorityOption[] (priority dropdown options)
- userOptions: User[] (assignee dropdown options)

Methods:
- onTitleChange(value: string) → void (handle title input changes)
- onDescriptionChange(value: string) → void (handle description changes)
- onSubmit() → void (emit form submission)
- updateCharacterCount(field: string, value: string) → void (update character counters)

HTML STRUCTURE (PSEUDO CODE ONLY)

<form [formGroup]="formGroup" (ngSubmit)="onSubmit()" class="task-form">
  <div class="form-group">
    <label for="title">Task Title *</label>
    <input-component 
      id="title"
      formControlName="title"
      placeholder="Enter task title"
      [maxLength]="255"
      (valueChange)="onTitleChange($event)">
    </input-component>
    <character-counter-component 
      [currentLength]="titleCharCount"
      [maxLength]="255"
      fieldName="title">
    </character-counter-component>
  </div>
  
  <div class="form-group">
    <label for="description">Description</label>
    <textarea-component 
      id="description"
      formControlName="description"
      placeholder="Enter task description"
      [maxLength]="2000"
      rows="4"
      (valueChange)="onDescriptionChange($event)">
    </textarea-component>
    <character-counter-component 
      [currentLength]="descriptionCharCount"
      [maxLength]="2000"
      fieldName="description">
    </character-counter-component>
  </div>
  
  <div class="form-group">
    <label for="priority">Priority *</label>
    <select-component 
      id="priority"
      formControlName="priority"
      [options]="priorityOptions"
      placeholder="Select priority">
    </select-component>
  </div>
  
  <div class="form-group">
    <label for="dueDate">Due Date *</label>
    <date-picker-component 
      id="dueDate"
      formControlName="dueDate"
      [minDate]="tomorrow"
      placeholder="Select due date">
    </date-picker-component>
  </div>
  
  <div class="form-group">
    <label for="assignedTo">Assignee</label>
    <select-component 
      id="assignedTo"
      formControlName="assignedTo"
      [options]="userOptions"
      placeholder="Select assignee">
    </select-component>
  </div>
</form>

CSS SPECIFICATION

Layout:
- display: flex, flex-direction: column
- gap: 1.5rem between form groups

Spacing:
- .form-group: margin-bottom 1.5rem
- label: margin-bottom 0.5rem, font-weight 500
- input spacing: padding 0.75rem

Component Styles:
- .task-form: width 100%
- .form-group: display flex, flex-direction column
- labels: color text-gray-700, font-size 0.875rem
- required indicator (*): color red-500

Responsive:
- @media (max-width: 480px): reduced padding and font sizes

API INTEGRATION

- None (pure form component, parent handles API calls)

ERROR HANDLING

- Form validation errors → Display via ValidationFeedbackComponent
- Required field validation → Show "This field is required"
- Max length validation → Show "Maximum X characters allowed"
- Date validation → Show "Please select a future date"

INTERACTION FLOW

User Action → UI → API
- Type in title field → Update character count → Trigger validation → No API call
- Select priority → Update form control → Trigger validation → No API call
- Submit form → Emit formSubmit event → Parent handles API call

### 4.3 ValidationFeedbackComponent

Component Type: Shared

Mapped From A1: ValidationFeedbackComponent

Purpose: Display real-time validation messages for form fields

Parent: TaskCreationComponent, TaskFormComponent

Children: None

Dependencies:
- Angular Reactive Forms
- Validation Messages Constants

Library Components Used: None

TypeScript Specification

Inputs:
- field: AbstractControl (form control to validate)
- fieldName: string (field identifier)
- validationRules: ValidationRule[] (validation rule definitions)

Outputs: None

State:
- currentErrors: string[] (current validation error messages)
- isVisible: boolean (whether to show validation feedback)

Methods:
- getErrorMessage(errorKey: string, errorValue: any) → string (get localized error message)
- shouldShowError() → boolean (determine if errors should be displayed)

HTML STRUCTURE (PSEUDO CODE ONLY)

<div class="validation-feedback" *ngIf="shouldShowError()">
  <div class="error-message" *ngFor="let error of currentErrors">
    <icon name="warning" class="error-icon"></icon>
    <span class="error-text">{{ error }}</span>
  </div>
</div>

CSS SPECIFICATION

Layout:
- display: flex, flex-direction: column
- gap: 0.25rem between error messages

Spacing:
- margin-top: 0.25rem
- padding: 0.5rem

Component Styles:
- .validation-feedback: background red-50, border red-200
- .error-message: display flex, align-items center, gap 0.5rem
- .error-icon: color red-500, size 1rem
- .error-text: color red-700, font-size 0.875rem

Responsive:
- Consistent across all breakpoints

API INTEGRATION

- None (pure validation display component)

ERROR HANDLING

- Graceful handling of missing validation rules
- Default error messages for unknown validation types

INTERACTION FLOW

User Action → UI → API
- Form field loses focus → Check validation → Display errors if any → No API call
- Form field value changes → Real-time validation → Update error display → No API call

### 4.4 CharacterCounterComponent

Component Type: Shared

Mapped From A1: CharacterCounterComponent

Purpose: Real-time character counting for text input fields

Parent: TaskCreationComponent, TaskFormComponent

Children: None

Dependencies: None

Library Components Used: None

TypeScript Specification

Inputs:
- currentLength: number (current character count)
- maxLength: number (maximum allowed characters)
- fieldName: string (field identifier for styling)

Outputs: None

State:
- isNearLimit: boolean (80% of max length reached)
- isOverLimit: boolean (exceeds max length)
- percentageUsed: number (percentage of max length used)

Methods:
- calculatePercentage() → number (calculate usage percentage)
- getCounterClass() → string (get CSS class based on usage)

HTML STRUCTURE (PSEUDO CODE ONLY)

<div class="character-counter" [ngClass]="getCounterClass()">
  <span class="counter-text">
    {{ currentLength }} / {{ maxLength }}
  </span>
  <div class="counter-bar" *ngIf="showProgressBar">
    <div class="counter-progress" [style.width.%]="percentageUsed"></div>
  </div>
</div>

CSS SPECIFICATION

Layout:
- display: flex, flex-direction: column
- align-items: flex-end

Spacing:
- margin-top: 0.25rem
- gap: 0.25rem

Component Styles:
- .character-counter: font-size 0.75rem
- .counter-text: color gray-500 (normal), orange-500 (near limit), red-500 (over limit)
- .counter-bar: width 100%, height 2px, background gray-200
- .counter-progress: height 100%, transition width 0.2s

Responsive:
- Consistent across all breakpoints

API INTEGRATION

- None (pure UI component)

ERROR HANDLING

- Handle negative values gracefully
- Handle undefined/null inputs

INTERACTION FLOW

User Action → UI → API
- Type in text field → Update character count → Visual feedback changes → No API call

### 4.5 LoadingSpinnerComponent

Component Type: Shared

Mapped From A1: LoadingSpinnerComponent

Purpose: Loading state indicator during API calls and async operations

Parent: TaskCreationComponent, other components with async operations

Children: None

Dependencies: None

Library Components Used: None

TypeScript Specification

Inputs:
- isVisible: boolean (controls spinner visibility)
- message?: string (optional loading message)
- size?: 'sm' | 'md' | 'lg' (spinner size variant)
- overlay?: boolean (whether to show backdrop overlay)

Outputs: None

State: None (stateless component)

Methods: None

HTML STRUCTURE (PSEUDO CODE ONLY)

<div class="loading-spinner-container" *ngIf="isVisible">
  <div class="spinner-overlay" *ngIf="overlay"></div>
  <div class="spinner-content">
    <div class="spinner" [ngClass]="'spinner-' + size">
      <div class="spinner-circle"></div>
    </div>
    <p class="spinner-message" *ngIf="message">{{ message }}</p>
  </div>
</div>

CSS SPECIFICATION

Layout:
- position: absolute (overlay mode) or relative
- display: flex, justify-content: center, align-items: center
- flex-direction: column

Spacing:
- gap: 1rem between spinner and message
- padding: 2rem

Component Styles:
- .spinner-overlay: position fixed, inset 0, background rgba(0,0,0,0.5), z-index 1000
- .spinner: border-radius 50%, animation spin 1s linear infinite
- .spinner-sm: width 1rem, height 1rem
- .spinner-md: width 2rem, height 2rem
- .spinner-lg: width 3rem, height 3rem
- .spinner-message: color gray-600, font-size 0.875rem

Responsive:
- Consistent across all breakpoints

API INTEGRATION

- None (pure UI component)

ERROR HANDLING

- Graceful handling of missing props
- Default values for optional props

INTERACTION FLOW

User Action → UI → API
- API call starts → Show spinner → No direct user interaction
- API call completes → Hide spinner → No direct user interaction

5. COMPONENT MAPPING SUMMARY

| A1 Component | Final Component | Type |
|--------------|----------------|---------|
| TaskCreationComponent | TaskCreationComponent | Feature |
| TaskFormComponent | TaskFormComponent | Feature |
| ValidationFeedbackComponent | ValidationFeedbackComponent | Shared |
| CharacterCounterComponent | CharacterCounterComponent | Shared |
| LoadingSpinnerComponent | LoadingSpinnerComponent | Shared |
| ButtonComponent | ButtonComponent (from library) | Shared |
| ModalComponent | ModalComponent | Shared |
| HeaderComponent | HeaderComponent | Layout |
| SidebarComponent | SidebarComponent | Layout |
| KanbanBoardComponent | KanbanBoardComponent | Feature |

6. DATA FLOW (FROM AGENT-1)

Parent → Child:
- App → TaskCreationComponent (isOpen: boolean, onClose: function, onTaskCreated: function)
- TaskCreationComponent → TaskFormComponent (formGroup: FormGroup, isLoading: boolean)
- TaskCreationComponent → ValidationFeedbackComponent (field: AbstractControl, fieldName: string, validationRules: ValidationRule[])
- TaskCreationComponent → CharacterCounterComponent (currentLength: number, maxLength: number, fieldName: string)
- TaskCreationComponent → LoadingSpinnerComponent (isVisible: boolean, message: string)
- TaskCreationComponent → ButtonComponent (variant: string, processing: boolean, disabled: boolean, label: string)

Child → Parent:
- TaskFormComponent → TaskCreationComponent (formSubmit: form data, characterCountChanged: count updates)
- ValidationFeedbackComponent → TaskCreationComponent (validation state changes)
- ButtonComponent → TaskCreationComponent (userClick: click events)
- TaskCreationComponent → App (taskCreated: Task object, formClosed: void)

7. API USAGE SUMMARY

Component → API:
- TaskCreationComponent → POST /api/v1/tasks (task creation)
  - Request: CreateTaskRequest with title, description, priority, dueDate, assignedTo, tags, estimatedHours
  - Response: 201 Created with Task object
  - Error Handling: 400 (validation), 401 (auth), 403 (permissions), 409 (conflicts), 429 (rate limit), 500 (server error)

API Field Mapping:
- title (form) → title (API) [required, maxLength 200]
- description (form) → description (API) [maxLength 2000]
- priority (form) → priority (API) [enum: LOW, MEDIUM, HIGH, CRITICAL]
- dueDate (form) → dueDate (API) [ISO 8601 format, future date]
- assignedTo (form) → assignedTo (API) [UUID format]
- tags (form) → tags (API) [array of strings, max 10 items]
- estimatedHours (form) → estimatedHours (API) [number, min 0.5, max 1000]

Authentication:
- Bearer JWT token required for all API calls
- Token validation and refresh handling
- Automatic logout on 401 responses

8. NOTES & CONSTRAINTS

- HTML must follow Agent-1 structure strictly as the single source of truth
- Do NOT create new components unnecessarily - reuse existing library components
- Prefer reusable library components from uicomplibraryplay
- HTML must be pseudo-code ONLY (NOT real framework code)
- CSS should include only key classes and layout rules, not full stylesheets
- All form validation must be real-time with immediate feedback
- Character counters must update on every keystroke
- API integration must handle all error scenarios gracefully
- Loading states must be implemented for all async operations
- Form must reset after successful task creation
- Modal must close after successful submission
- Accessibility requirements must be met (ARIA labels, keyboard navigation)
- Component testing must be implemented for all new components
- Performance optimization required for real-time validation
- Responsive design must work across all device sizes
- Form data must be validated both client-side and server-side
- Error messages must be user-friendly and actionable
- Success feedback must be clear and immediate
- Form state must be preserved during validation errors
- API retry logic must be implemented for transient failures
- Component documentation must be comprehensive for future maintenance